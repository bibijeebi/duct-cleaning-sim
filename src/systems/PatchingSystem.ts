import { Scene } from '@babylonjs/core';

export class PatchingSystem {
  private _scene: Scene;

  constructor(scene: Scene) {
    this._scene = scene;
  }

  init(): void {
    // TODO: Access hole cutting, patching to code
  }

  dispose(): void {
    // TODO: Clean up
  }
}
